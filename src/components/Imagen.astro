---
interface Props {
	src: string;
  type: string;
}

const { src, type } = Astro.props;
---

<img
  src={"/"+src}
  alt={src}
  class={"carousel-image"+type+" flex-0 flex-grow-0 w-full h-auto box-border rounded-lg"}
/>
